{
  "name": "cysecnotes",
  "version": "1.0.0",
  "private": true,
  "description": "CySecNotes — HKIIT 網絡安全筆記庫（AI 輔助整理的雙語應考筆記）",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-notes.mjs",
    "dev": "npm run sync && vitepress dev docs",
    "build": "npm run sync && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
